from contentshield.detector.base import BaseToxicDetector

class ImageToxicDetector(BaseToxicDetector): 

    def __init__(self): 
        ...


    def detect(self, content):
        return super().detect(content)